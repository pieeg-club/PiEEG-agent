# dependencies
node_modules/

# logs & caches
*.log
.vite/
.DS_Store

# Note: dist/ is committed so users don't need to build the frontend
# Only ignore it during development if you want clean git status
